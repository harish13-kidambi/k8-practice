apiVersion: v1
kind: Service
metadata: 
  name: service-pod
  labels:
    name: service-pod
    environment: dev
spec:
  selector:
    name: env-pod
  ports:
  - name: http
    protocol: TCP
    port: 80 #service port
    targetPort: 80 #pod port

